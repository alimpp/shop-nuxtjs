export interface IAddress {
  name: string;
  content: string;
  postalCode: string;
  userId: string;
  created_at: Date;
}

export interface IAddBody {
  name: string;
  content: string;
  postalCode: string;
}
